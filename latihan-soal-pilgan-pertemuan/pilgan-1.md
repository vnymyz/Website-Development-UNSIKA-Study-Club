# Kuis Pilihan Ganda — Pertemuan 1: Fondasi Web & HTML

> Dokumen ini untuk kebutuhan internal (disiapkan moderator ke Slido untuk ditampilkan live saat kelas, lihat `cara-import-slido.md`). Jawaban benar ditandai ✅ — jangan dibagikan mentah ke peserta sebelum kuis berlangsung.

## Soal 1
Dalam analogi warung makan, browser (client) berperan sebagai...

A. Warung yang menyiapkan makanan
B. Pembeli yang memesan makanan ✅
C. Menu yang dipajang di warung
D. Kasir yang menerima pembayaran

## Soal 2
Apa yang dikirim server sebagai balasan (response) saat browser meminta sebuah halaman web?

A. Hanya alamat IP server
B. Password akses ke server
C. HTML, CSS, dan JavaScript milik halaman itu ✅
D. Daftar semua pengguna yang sedang online

## Soal 3
Manakah pernyataan yang paling tepat tentang peran HTML dalam sebuah halaman web?

A. HTML menentukan warna dan tata letak halaman
B. HTML menentukan struktur dan isi halaman ✅
C. HTML menentukan interaksi seperti klik tombol
D. HTML mengatur koneksi ke database

## Soal 4
Kalau sebuah halaman web hanya punya HTML tanpa CSS sama sekali, apa yang terjadi?

A. Halaman tidak bisa dibuka sama sekali
B. Halaman tetap tampil, tapi polos tanpa styling ✅
C. Browser akan menampilkan pesan error
D. Semua teks otomatis hilang

## Soal 5
Apa fungsi tag `<meta charset="UTF-8">` di dalam `<head>`?

A. Menentukan judul yang muncul di tab browser
B. Menentukan warna latar halaman
C. Memastikan karakter khusus (seperti é atau Rp) tampil dengan benar ✅
D. Menghubungkan file CSS ke HTML

## Soal 6
Tag semantik mana yang paling tepat dipakai untuk membungkus kumpulan link navigasi?

A. `<section>`
B. `<nav>` ✅
C. `<article>`
D. `<footer>`

## Soal 7
Apa perbedaan utama antara `<ul>` dan `<ol>`?

A. `<ul>` untuk list bernomor, `<ol>` untuk list tanpa nomor
B. `<ul>` untuk list tanpa urutan, `<ol>` untuk list berurutan/bernomor ✅
C. `<ul>` hanya untuk gambar, `<ol>` hanya untuk teks
D. Keduanya persis sama, hanya beda nama

## Soal 8
Pada form berikut, apa fungsi atribut `for="nama"` di dalam tag `<label>`?

```html
<label for="nama">Nama</label>
<input type="text" id="nama">
```

A. Menentukan warna label
B. Menghubungkan label dengan input yang punya `id` sama, supaya klik label langsung fokus ke input ✅
C. Membuat input menjadi wajib diisi
D. Membatasi jumlah karakter yang boleh diketik

## Soal 9
Apa kegunaan ekstensi Live Server di VS Code?

A. Untuk menghubungkan ke database Supabase
B. Untuk mengecek ejaan pada teks HTML
C. Untuk menjalankan halaman secara lokal dan otomatis me-refresh saat file disimpan ✅
D. Untuk otomatis meng-upload kode ke GitHub

## Soal 10
Manakah urutan tag yang benar untuk struktur dasar sebuah dokumen HTML?

A. `<body>` di dalam `<head>`, lalu keduanya di dalam `<html>`
B. `<html>` berisi `<head>` dan `<body>` sebagai dua bagian sejajar ✅
C. `<head>` dan `<body>` tidak boleh ada dalam satu file yang sama
D. `<html>` harus diletakkan di dalam `<body>`
